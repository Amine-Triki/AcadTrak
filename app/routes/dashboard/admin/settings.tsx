import { Card, Typography } from "antd";

const { Title, Text } = Typography;

export default function AdminSettingsPage() {
	return (
		<Card>
			<Title level={4}>الإعدادات</Title>
			<Text type="secondary">صفحة الإعدادات قيد التطوير.</Text>
		</Card>
	);
}
